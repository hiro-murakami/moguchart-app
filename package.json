{
  "name": "moguchart-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "deploy:all": "pnpm run build && firebase deploy",
    "deploy:functions": "pnpm run build && firebase deploy --only functions",
    "deploy:frontend": "pnpm run build && firebase deploy --only hosting",
    "serve:functions": "firebase emulators:start --only functions",
    "refresh:packages": "rm -rf node_modules pnpm-lock.yaml packages/frontend/node_modules packages/functions/node_modules && pnpm i && pnpm --filter ./packages/functions run prisma:generate"
  },
  "devDependencies": {
    "npm-run-all2": "^8.0.4",
    "prettier": "^3.8.0"
  }
}
