{
  "name": "moguchart-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "deploy": "firebase deploy"
  },
  "devDependencies": {
    "npm-run-all2": "^8.0.4"
  }
}
